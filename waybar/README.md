# waybar

Status bar configuration and styling. Stows into `~/.config/waybar/`.

Custom modules use scripts from the `bin` module (`waybar-cpustatus`,
`waybar-gpustatus`).

## Dependencies

- waybar
- pavucontrol
- python3
- nvidia-smi (optional, for GPU status module)
