#!/usr/bin/env bash
# shellcheck shell=bash

weather=$(curl -sf "wttr.in/?format=%C+%t+%h+%w" 2>/dev/null)
if [ -z "$weather" ]; then
  notify-send -u low "Weather unavailable"
  exit 0
fi

notify-send -u low "$weather"
