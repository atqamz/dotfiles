#!/usr/bin/env bash
# shellcheck shell=bash
script_dir="$(dirname "$(readlink -f "$0")")"
source "${BASH_COMMON:-$script_dir/bash-common}"

require_cmd notify-send

if toggle suspend-off; then
  notify-send -u low "Auto-suspend disabled"
else
  notify-send -u low "Auto-suspend enabled"
fi
